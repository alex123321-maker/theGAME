# Backlog ISO Runtime

Operational checklist for this project line:

1. `ISO-01` projection contract isolated in `world_projection.gd`.
2. `RUN-01` explicit runtime panel controls for seed and diagnostics.
3. `RUN-02` stable regenerate/randomize/apply pipeline preserving scene state.
4. `ISO-02` isometric renderer active.
5. `ISO-03` stable visual ordering (`z_index` + Y-sort where needed).
6. `ISO-04` minimal isometric assets package in repo.
7. `ISO-05` resolver mapping `MapData -> tile textures` with missing mapping logs.
8. `ISO-06` presentation-only props layer.
9. `RUN-03` diagnostic HUD with validation status and summary.
10. `RUN-04` reference seed regression loop and screenshot support.
11. `UX-01` screen-to-logical hover tile selection.
12. `DOC-01` docs aligned with runtime-isometric workflow.
